import { CheckDataDirective } from './check-data.directive';

describe('CheckDataDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckDataDirective();
    expect(directive).toBeTruthy();
  });
});
